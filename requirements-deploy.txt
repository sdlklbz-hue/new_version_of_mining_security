# Docker API 部署：推理 + API 内触发训练
# 在 serve 依赖基础上补齐模型反序列化与训练栈。

-r requirements-serve.txt
-r requirements-train.txt

# 当前 stacking_risk_v1_stable.pkl 的树模型基学习器
xgboost>=2.0.0
lightgbm>=4.0.0
catboost>=1.2.0

# 含 MLP/CNN 基学习器时需 TensorFlow 反序列化
tensorflow>=2.13.0
