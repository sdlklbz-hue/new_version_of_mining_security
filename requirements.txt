# 后端 API 核心精简依赖
# 仅包含 FastAPI 服务与基础 sklearn/pandas 运行时。
# Docker 部署真实模型推理默认使用 requirements-deploy.txt；
# 如需完整训练 / RAG / 旧 Streamlit 前端，请安装 requirements-full.txt。

# Web 框架
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# 决策工作流与 LLM 客户端
langgraph>=0.2.0
langchain>=0.2.0
openai>=1.0.0

# 数据库与存储
GitPython>=3.1.0

# API 运行时模型 / 特征工程基础
scikit-learn>=1.3.0
scipy>=1.10.0
joblib>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# 配置、日志、Token 计数
pydantic>=2.0.0
pyyaml>=6.0.1
python-json-logger>=2.0.7
tiktoken>=0.5.0

# 后端 HTTP/Webhook 与模板
requests>=2.31.0
jinja2>=3.1.0
tqdm>=4.66.0
